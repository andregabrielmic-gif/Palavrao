// Versão Final: script.js
const words = [ "sagaz","âmago","termo","êxito","mexer","nobre","senso","afeto","ética","algoz","plena","fazer","assim","tênue","mútua","sobre","seção","vigor","poder","sutil","porém","fosse","cerne","ideia","sanar","audaz","moral","inato","desde","muito","justo","sonho","honra","torpe","razão","amigo","ícone","etnia","fútil","anexo","dengo","haver","lapso","então","expor","tempo","boçal","hábil","mútuo","saber","casal","graça","xibiu","dizer","ardil","pesar","estar","dever","causa","tenaz","sendo","ainda","pária","temor","brado","genro","comum","ápice","posse","prole","ânimo","ceder","corja","pauta","detém","fugaz","censo","ânsia","culto","atroz","digno","mundo","forte","mesmo","vulgo","vício","saúde","criar","cozer","todos","revés","valha","jeito","pudor","denso","dogma","neném","louco","atrás","regra","ordem","limbo","feliz","pedir","homem","mercê","impor","ajuda","banal","coisa","juízo","forma","legal","sábio","falso","certo","falar","servo","prosa","pífio","desse","presa","posso","cunho","herói","devir","viril","vendo","fácil","ontem","valor","visar","linda","manso","sério","meiga","puder","mágoa","acaso","fluir","afago","ímpio","lugar","temer","abrir","praxe","união","obter","gerar","burro","matiz","óbvio","afins","cisma","bruma","êxodo","pleno","crise","vênia","álibi","tédio","fluxo","ritmo","morte","senil","havia","levar","olhar","enfim","casta","tomar","visão","gênio","prumo","ouvir","brega","reles","falta","vital","parto","calma","bravo","favor","outro","tecer","vivaz","terra","reter","sábia","ameno","viver","tendo","único","força","valia","laico","passo","grato","achar","noção","nicho","rever","carma","nossa","papel","ranço","pobre","rogar","noite","façam","prime","dúbio","fardo","farsa","ativo","coeso","fator","épico","anelo","claro","óbito","selar","líder","leigo","sinto","citar","cisão","cesta","ciúme","sonso","vazio","deter","atuar","velho","gente","ficar","tende","haste","adiar","humor","revel","ideal","fonte","ponto","árduo","labor","igual","marco","senão","remir","terno","exato","feixe","vemos","amplo","hiato","tanto","capaz","lavra","cauda","débil","relva","ciclo","jovem","ótica","varão","gesto","chuva","ambos","raiva","pouco","coçar","caçar","toada","sonsa","vácuo","velar","apoio","imune","série","vimos","xeque","algum","farão","feito","horda","fusão","carro","entre","leito","coesa","minha","sente","cruel","trama","sorte","doido","frase","lazer","brisa","ímpar","verso","torço","chata","rigor","massa","botar","prece","pegar","maior","dorso","seita","fauna","moção","signo","furor","áurea","preso","covil","livro","agora","credo","plano","vetor","comer","flora","casto","saiba","morar","praia","adeus","nunca","dócil","aliás","peste","houve","árido","setor","ambas","ardor","manha","mudar","peixe","parte","visse","rezar","vírus","risco","meses","salvo","vulto","junto","saída","breve","campo","vasto","ótimo","aceso","grupo","estão","morro","antes","sinal","reger","lenda","andar","conta","áureo","prado","anais","acima","opção","serão","verbo","festa","segue","fugir","chulo","vilão","rapaz","leite","birra","motim","nação","texto","treta","brava","índio","tirar","parar","átomo","fitar","ídolo","puxar","jazia","traga","tenso","reino","gerir","filho","alude","átrio","prazo","tosco","prova","turba","norma","bônus","época","exame","manhã","voraz","corpo","acesa","sarça","preto","cheio","bando","aonde","malta","arcar","ligar","nosso","fatos","magia","quase","anciã","cópia","avião","fatal","certa","longe","dessa","praga","afora","nível","fixar","oásis","sexta","mente","apego","pompa","lidar","perda","apelo","tocar","coito","alado","parca","caixa","sumir","verme","fraco","livre","tinha","solda","vezes","porta","lindo","firme","grave","solto","bater","opaco","faixa","astro","salve","irmão","sabia","besta","virão","turva","doído","atual","trupe","fenda","navio","elite","deixa","grata","pardo","exijo","alçar","autor","junto","pique","curso","viria","bicho","bioma","macio","desta","pagão","aluno","ético","chato","coisa","ficha","menos","calda","posto","abuso","rádio","caber","vídeo","supor","culpa","judeu","lápis","verba","zelar","super","gosto","suave","calão","retém","extra","agudo","molho","torso","baixo","vosso","piada","facho","peito","vinha","turma","passa","pódio","sítio","ruína","asilo","combo","traço","órfão","ávida","estio","pilar","turvo","louça","chama","ações","mosto","forem","refém","pisar","páreo","amena","poeta","mesma","bravo","museu","ereto","finda","fenda","lasso","local","meigo","optar","medir","drama","busca","teste","poema","tento","surja","autos","clima","folga","rumor","coral","aviso","rouca","geral","paira","cocho","amiga","calmo","pedra","boato","idoso","feroz","tacha","urgia","cetro","volta","rubro","pacto","feudo","móvel","crime","lição","monge","açude","golpe","ateia","daqui","tetra","ponha","ecoar","ébano","carta","corso","casar","natal","monte","falha","cacho","saldo","aroma","verde","plumo","vigia","itens","conto","briga","fazia","vetar","hoste","grama","tribo","pasmo","tarde","fórum","manga","letal","amada","única","troça","rival","ornar","chefe","vento","sósia","civil","fruto","roupa","venha","úteis","penta","nuvem","pinho","tchau","órgão","plebe","areia","jogar","vazão","cargo","virar","nesse","lesse","jejum","berro","arado","macro","magna","fosso","axila","finjo","perto","gíria","rocha","mídia","farta","beijo","tiver","varoa","légua","troca","tutor","bruto","todas","calor","bruta","traje","deste","renda","gabar","trato","pomar","assar","tenha","porte","surto","perco","amado","guria","vadio","viram","nessa","dança","santo","verão","âmbar","rural","feita","xucro","nesta","odiar","canso","lagos","depor","mamãe","vista","fossa","laudo","aviar","bazar","canto","vedar","marca","pavor","chula","etapa","recém","cheia","negar","salmo","bolsa","irado","irada","cerca","certo","cifra","densa","minar","ágape","clero","visto","vista","cinto","coroa","bucho","vagar","horto","horta","burra","burro","invés","molde","letra","ruído","largo","larga","lesão","folha","quais","prato","sótão","paiol","velha","final","penso","trago","traga","funda","fundo","deram","pasma","vasta","vasto","queda","podar","olhos","troço","linha","piche","troco","troca","úmido","frota","preço","folia","neste","áudio","ileso","peita","peito","outra","outro","farol","resto","resta","disso","disto","manto","manta","matar","monta","cosmo","redor","chave","seiva","barro","cível","bolso","bolsa","mover","misto","mista","falsa","falso","lábia","lábio","retro","vazia","vazio","limpo","limpa","louco","louca","nariz","veloz","justa","justo","barão","louro","loira","lutar","álbum","mimar","macho","sabor","samba","gemer","dados","punha","toque","axial","lucro","porca","porco","arroz","longo","longa","zumbi","calça","enjoo","rente","calvo","calva","venho","venha","findo","salva","salvo","subir","farto","farta","urgir","diabo","pagar","pagou","lousa","baixa","baixo","pluma","firma","solta","sacar","sabiá","sábio","ousar","valer","sexto","bruxa","forro","torna","fazes","sigla","fátuo","repor","reler","fugiu","lento","gueto","hífen","bugre","focar","pular","demão","canil","corte","corar","custo","feira","mania","versa","míope","sadio","ferir","sócio","harém","tumba","sugar","tênis","digna","penca","ceifa","bolos","doces","disco","circo","rouco","pisca","prata","brasa","broxa","altar","rampa","fruta","blusa","pizza","sushi","torta","caldo","vinho","limão","mamão","trigo","cobra","touro","cabra","pônei","mosca","vespa","garça","lesma","polvo","touca","pulso","dedos","ombro","ossos","unhas","dente","testa","praça","pátio","jarda","selva","túnel","brejo","piano","balde","banco","tampa","rolha","cinza","apito","lança","corda","régua","molas","azedo","rouco","idosa","úteis","magma","susto","ritmo","greve","clone","apoio"];

let activeMode = 'solo';
let isAnimating = false;
const wordLength = 5;

const gameState = {
    solo: { targets: [], solved: [], currentRow: 0, currentCol: 0, boardState: [], maxRows: 6, keyboardState: {} },
    dueto: { targets: [], solved: [], currentRow: 0, currentCol: 0, boardState: [], maxRows: 7, keyboardState: {} }
};

const keyboard = document.getElementById("keyboard");
const tabSolo = document.getElementById("tab-solo");
const tabDueto = document.getElementById("tab-dueto");
const soloContainer = document.getElementById("solo-container");
const duetoContainer = document.getElementById("dueto-container");
const gameBoards = {
    solo: [document.getElementById("game-solo")],
    dueto: [document.getElementById("game-dueto1"), document.getElementById("game-dueto2")]
};

function normalize(str) { return str.normalize("NFD").replace(/[\u0300-\u036f]/g, ""); }
const PRIORITY = { unset: -1, absent: 0, present: 1, correct: 2 };

function updateKeyboard(letter, status) {
    const state = gameState[activeMode];
    const normalizedLetter = normalize(letter).toLowerCase();
    const currentPriority = PRIORITY[state.keyboardState[normalizedLetter] || 'unset'];
    const newPriority = PRIORITY[status];

    if (newPriority > currentPriority) {
        state.keyboardState[normalizedLetter] = status;
    }
}

function updateKeyboardState() {
    const state = gameState[activeMode];
    document.querySelectorAll(".key").forEach(key => {
        const char = key.id.replace('key-', '');
        const status = state.keyboardState[char] || 'unset';
        key.classList.remove('correct', 'present', 'absent');
        if (status !== 'unset') {
            key.classList.add(status);
        }
    });
}

function getStatuses(guess, target) {
    const g = normalize(guess).toLowerCase();
    const t = normalize(target).toLowerCase();
    const status = new Array(wordLength).fill("absent");
    const counts = {};
    for (let ch of t) { counts[ch] = (counts[ch] || 0) + 1; }
    for (let i = 0; i < wordLength; i++) {
        if (g[i] === t[i]) { status[i] = "correct"; counts[g[i]] -= 1; }
    }
    for (let i = 0; i < wordLength; i++) {
        if (status[i] === "correct") continue;
        if ((counts[g[i]] || 0) > 0) { status[i] = "present"; counts[g[i]] -= 1; }
    }
    return status;
}

function saveCurrentState() {
    if (!activeMode) return;
    const state = gameState[activeMode];
    const activeBoards = gameBoards[activeMode];

    state.boardState = [];
    activeBoards.forEach(board => {
        const boardData = [];
        const rows = board.querySelectorAll('.row');
        rows.forEach(row => {
            const rowData = [];
            const tiles = row.querySelectorAll('.tile');
            tiles.forEach(tile => {
                const back = tile.querySelector('.back');
                rowData.push({
                    letter: tile.querySelector('.front').textContent,
                    status: back.classList.contains('correct') ? 'correct' :
                        back.classList.contains('present') ? 'present' :
                        back.classList.contains('absent') ? 'absent' : null,
                    isFlipped: tile.classList.contains('flip')
                });
            });
            boardData.push(rowData);
        });
        state.boardState.push(boardData);
    });
}

function loadState(mode) {
    const state = gameState[mode];
    const activeBoards = gameBoards[mode];

    activeBoards.forEach((board, boardIndex) => {
        const boardData = state.boardState[boardIndex];
        const rows = board.querySelectorAll('.row');
        rows.forEach((row, rIndex) => {
            const tiles = row.querySelectorAll('.tile');
            tiles.forEach((tile, tIndex) => {
                const tileData = boardData[rIndex][tIndex];
                tile.querySelector('.front').textContent = tileData.letter;
                const back = tile.querySelector('.back');
                back.textContent = tileData.letter;

                back.classList.remove('correct', 'present', 'absent');
                tile.classList.remove('flip');

                if (tileData.status) {
                    back.classList.add(tileData.status);
                }
                if (tileData.isFlipped) {
                    tile.classList.add('flip');
                }
            });
        });
    });
    updateKeyboardState();
    updateSelection();
}

function switchGameMode(newMode) {
    if (activeMode === newMode) return;
    saveCurrentState();
    activeMode = newMode;

    if (newMode === 'solo') {
        soloContainer.style.display = 'block';
        duetoContainer.style.display = 'none';
        tabSolo.classList.add("active");
        tabDueto.classList.remove("active");
    } else {
        soloContainer.style.display = 'none';
        duetoContainer.style.display = 'flex';
        tabSolo.classList.remove("active");
        tabDueto.classList.add("active");
    }
    loadState(newMode);
}

function revealGuess(guess) {
    isAnimating = true;
    const state = gameState[activeMode];
    const activeBoards = gameBoards[activeMode];

    for (let i = 0; i < activeBoards.length; i++) {
        if (!state.solved[i]) {
            const statuses = getStatuses(guess, state.targets[i]);
            animateRowFlip(activeBoards[i], state.currentRow, statuses, guess);
            if (statuses.every(s => s === 'correct')) { state.solved[i] = true; }
        }
    }

    setTimeout(() => {
        isAnimating = false;
        if (state.solved.every(s => s === true)) {
            setTimeout(() => alert("Parabéns, você acertou tudo!"), 100);
            return;
        }
        if (state.currentRow >= state.maxRows - 1) {
            alert("Fim de jogo! As palavras eram: " + state.targets.join(", ").toUpperCase());
            return;
        }
        state.currentRow++;
        state.currentCol = 0;
        updateSelection();
    }, wordLength * 300 + 500);
}

function animateRowFlip(boardElement, rowIndex, statuses, guess) {
    const rowElement = boardElement.querySelectorAll(".row")[rowIndex];
    const tiles = Array.from(rowElement.children);
    tiles.forEach((tile, i) => {
        const back = tile.querySelector(".back");
        back.textContent = guess[i].toUpperCase();
        back.classList.add(statuses[i]);
        updateKeyboard(guess[i], statuses[i]);
        setTimeout(() => tile.classList.add("flip"), i * 300);
    });
    updateKeyboardState();
}

function handleKeyPress(key) {
    if (isAnimating) return;
    const state = gameState[activeMode];
    if (state.solved.every(s => s === true) || state.currentRow >= state.maxRows) return;

    const activeBoards = gameBoards[activeMode];
    const primaryBoard = activeBoards[0];
    const row = primaryBoard.querySelectorAll(".row")[state.currentRow];
    if (!row) return;

    if (key === "Backspace") {
        if (state.currentCol > 0) {
            state.currentCol--;
            activeBoards.forEach(board => {
                board.querySelectorAll(".row")[state.currentRow].children[state.currentCol].querySelector(".front").textContent = "";
            });
        }
    } else if (key === "Enter") {
        if (state.currentCol !== wordLength) {
            alert("Complete a palavra!");
            return;
        }
        let guess = "";
        const tiles = Array.from(row.children);
        for (let tile of tiles) {
            guess += tile.querySelector(".front").textContent;
        }
        guess = guess.toLowerCase();
        if (!words.some(w => normalize(w) === normalize(guess))) {
            alert("Palavra não encontrada!");
            return;
        }
        revealGuess(guess);
    } else if (/^[a-zA-ZÀ-ÿ]$/.test(key) && state.currentCol < wordLength) {
        activeBoards.forEach(board => {
            board.querySelectorAll(".row")[state.currentRow].children[state.currentCol].querySelector(".front").textContent = key.toUpperCase();
        });
        state.currentCol++;
    }
    updateSelection();
}

function updateSelection() {
    const state = gameState[activeMode];
    document.querySelectorAll(".front").forEach(f => f.classList.remove("selected"));
    if (state.currentCol < wordLength) {
        gameBoards[activeMode].forEach(board => {
            const tile = board.querySelectorAll(".row")[state.currentRow]?.children[state.currentCol];
            if (tile) tile.querySelector(".front").classList.add("selected");
        });
    }
}

function initialize() {
    Object.values(gameBoards).flat().forEach(container => {
        const maxRowsForBoard = 7; // Construir sempre 7 linhas para consistência visual
        for (let r = 0; r < maxRowsForBoard; r++) {
            const row = document.createElement("div"); row.className = "row";
            for (let c = 0; c < wordLength; c++) {
                const tile = document.createElement("div"); tile.className = "tile";
                tile.innerHTML = `<div class="front"></div><div class="back"></div>`;
                row.appendChild(tile);
            }
            container.appendChild(row);
        }
    });

    const layout = ["qwertyuiop", "asdfghjkl", "zxcvbnm"];
    layout.forEach(line => {
        const row = document.createElement("div"); row.className = "key-row";
        for (let char of line) {
            const key = document.createElement("div"); key.className = "key";
            key.id = "key-" + char; key.textContent = char;
            key.addEventListener('click', () => handleKeyPress(char));
            row.appendChild(key);
        }
        keyboard.appendChild(row);
    });

    ['solo', 'dueto'].forEach(mode => {
        const state = gameState[mode];
        const numTargets = (mode === 'solo') ? 1 : 2;
        for (let i = 0; i < numTargets; i++) {
            let newWord = words[Math.floor(Math.random() * words.length)];
            while (state.targets.includes(newWord)) {
                newWord = words[Math.floor(Math.random() * words.length)];
            }
            state.targets.push(newWord);
            state.solved.push(false);
        }
        for (let i = 0; i < numTargets; i++) {
            state.boardState.push(Array(7).fill().map(() => Array(wordLength).fill({ letter: '', status: null, isFlipped: false })));
        }
    });

    document.addEventListener("keydown", e => handleKeyPress(e.key));
    tabSolo.addEventListener("click", () => switchGameMode("solo"));
    tabDueto.addEventListener("click", () => switchGameMode("dueto"));

    loadState("solo");
}

initialize();
